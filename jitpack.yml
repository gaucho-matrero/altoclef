jdk:
  - openjdk16

before_install:
  - ./ensure-jitpack-java-17 install

install:
  - if ! ./ensure-java-17 use; then source ~/.sdkman/bin/sdkman-init.sh; fi
  - java -version
  - ./gradlew publishToMavenLocal
